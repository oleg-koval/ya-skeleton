sudo: required

services:
  - docker

language: node_js

install:
  - yarn --ignore-engines

script:
  - npm run ci
  - npm danger ci

node_js:
  - "8"
  - "10"

# Skip builds for tags
if: tag IS blank
