sudo: required

services:
  - docker

language: node_js

script:
  - npm run test:docker
  - yarn danger ci

after_success: npm run coverage

node_js:
  - "8"

# Skip builds for tags
if: tag IS blank
